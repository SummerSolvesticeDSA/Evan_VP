class Solution {
  public:
    bool armstrongNumber(int n) {
        // code here
    int arm=0;
    int num=n;
    while(n>0){
        int d=n%10;
        arm=arm+(d*d*d);
         n=n/10;

    }
    return (arm==num);
}

};
